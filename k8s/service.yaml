apiVersion: v1
kind: Service
metadata:
  name: ai-webapp
  namespace: default
spec:
  type: ClusterIP   # 👈 change this (no more LoadBalancer)
  selector:
    app: ai-webapp
  ports:
    - protocol: TCP
      port: 80       # Service port
      targetPort: 8081 # Container port
